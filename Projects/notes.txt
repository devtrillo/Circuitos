Compile C
	gcc -Wall <myName>.c -o <compiled name> -l wiringPi
	gcc -Wall <Mycode>.cpp -o <complileName>  -lstdc++
	
Setup Wifi
	sudo iwlist wlan0 scan
	sudo nano /etc/wpa_supplicant/wpa_supplicant.conf

	Enter this the order of the networks is the connectivity priority
		network={
    			ssid="testing"
	 		psk="testingPassword"
		}
		if the network does NOT have password use this instead
		network={
		    ssid="<Name of the net>"
		    psk=NONE
		}


Add python code to startUP
	<Got to the root folder of thescript>
	sudo chmod 755 <pythonscript>.py
	sudo update-rc.d <pythonscript>.py defaults
